# EcoPackAI — Environment Variables
# Copy this to .env and fill in your values
# The app runs without a database (CSV-only mode) if DB vars are not set.

# ── PostgreSQL Database ──────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecopackai
DB_USER=postgres
DB_PASSWORD=your_password_here

# ── Flask ────────────────────────────────────────────────────
FLASK_ENV=development
FLASK_SECRET_KEY=change_this_to_a_random_secret_key
FLASK_DEBUG=1

# ── For Render / Heroku ──────────────────────────────────────
# Set DATABASE_URL if using a managed Postgres addon:
# DATABASE_URL=postgresql://user:password@host:5432/dbname
PORT=5000
